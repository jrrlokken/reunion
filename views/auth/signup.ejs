<%- include('../includes/head.ejs') %>
   <%- include('../includes/navigation.ejs') %>

    <main>
        <div class="backdrop"></div>
        <% if (errorMessage) { %>
            <div class="user-message user-message--error"><%= errorMessage %> </div>
        <% } %> 
        <form class="signup-form" action="/signup" method="POST">
            <div class="form-control">
                <input class="<%= validationErrors.find(e => e.param === 'email') ? 'invalid' : '' %>" type="email" name="email" id="email" value="<%= userInput.email %>">
                <label for="email">E-Mail</label>
            </div>
            <div class="form-control">
                <input class="<%= validationErrors.find(e => e.param === 'email') ? 'invalid' : '' %>" type="password" name="password" id="password" value="<%= userInput.password %>">
                <label for="password">Password</label>
            </div>
            <div class="form-control">
                <input class="<%= validationErrors.find(e => e.param === 'email') ? 'invalid' : '' %>" type="password" name="confirmPassword" id="confirmPassword" value="<%= userInput.confirmPassword %>">
                <label for="confirmPassword">Confirm Password</label>
            </div>

            <div class="button-container margin-top">
                <button class="btn btn-primary" type="submit">Sign Up</button>
            </div>
            <input type="hidden" name="_csrf" value="<%=  csrfToken %>">
        </form>
    </main>
<%- include('../includes/end.ejs') %>